{
    "name": "cloudflare-workers-booking",
    "version": "1.0.0",
    "description": "package for creating workers templates",
    "main": "index.js",
    "scripts": {
        "kvupload": "wrangler kv:key put --binding=STATIC 'main.js' static/main.js --path",
        "test": "echo \"Error: no test specified\" && exit 1",
        "format": "prettier --write '**/*.{js,css,json,md}'"
    },
    "author": "jaimelias",
    "license": "MIT",
    "devDependencies": {
        "prettier": "^1.17.0"
    },
    "dependencies": {
        "marked": "^1.2.5",
        "redux": "^4.0.5",
        "redux-logger": "^3.0.6",
        "redux-thunk": "^2.3.0",
        "serverless-cloudflare-workers": "^1.2.0"
    }
}
